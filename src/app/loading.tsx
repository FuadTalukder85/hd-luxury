import React from "react";

const Loading = () => {
  return (
    <div className="flex items-center justify-center min-h-[400px]">
      <p className="text-seaBlue font-semibold text-lg">Loading...</p>
    </div>
  );
};

export default Loading;
